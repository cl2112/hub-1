<div class="container">
	<div class="row">
		<h1 class="text-center">---Hub-2---</h1>
		{{#each article}}
		<div class="col-xs-12 well well-lg well-margin">
			<a href="/hub-3/{{title}}"> <h1>{{title}}</h1> </a>
			<h3>{{blurb}}</h3>
			<a href="{{link}}" target="_blank"><h4>{{link}}</h4></a>
			<img class="img-responsive" src="{{pic}}">
		</div>
		{{/each}}
	</div>
</div>